---
title: Overview
description: What Matcha is and how it works.
---

import { ManualPageNav } from '@/components/ManualPageNav'

<div className="home-flow">

# Overview

Matcha is a CLI tool that measures GPU energy consumption during AI training runs.

You prefix your training command with `matcha run`. Your training executes at full speed. When it finishes, Matcha reports how much energy your GPUs consumed.

## Who it's for

Engineers and researchers running training or fine-tuning jobs on NVIDIA GPUs, on cloud instances like RunPod, Lambda, and AWS, on-premise clusters, or local workstations.

## What it measures

- Total energy consumed (`joules`, `watt-hours`)
- Average and peak GPU power draw (`watts`)
- Duration of the full run
- Per-step energy breakdown (with `matcha wrap`)

## How it works

Matcha spawns your training command as a child process. In a separate background thread, it polls GPU power via NVML at 100ms intervals. Your training process runs natively. Matcha never intercepts stdout, modifies your code, or injects anything into your training loop.

<div className="home-diagram">
  <svg width="100%" viewBox="0 0 680 200" xmlns="http://www.w3.org/2000/svg" aria-label="How Matcha works">
    <style>
      {`
        text { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif; }
        .title { font-size: 13px; font-weight: 500; fill: #2C2C2A; }
        .sub { font-size: 11px; font-weight: 400; fill: #5F5E5A; }
        .box { fill: #F1EFE8; stroke: #D3D1C7; stroke-width: 0.5; }
        .accent { fill: #E8F5E0; stroke: #9CC78C; stroke-width: 0.5; }
      `}
    </style>

    <defs>
      <marker id="overview-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
        <path d="M2 1L8 5L2 9" fill="none" stroke="#B4B2A9" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" />
      </marker>
    </defs>

    <rect className="accent" x="40" y="30" width="120" height="56" rx="8" />
    <text className="title" x="100" y="52" textAnchor="middle" dominantBaseline="central">matcha run</text>
    <text className="sub" x="100" y="70" textAnchor="middle" dominantBaseline="central">your command</text>

    <line x1="160" y1="58" x2="192" y2="58" stroke="#B4B2A9" strokeWidth="0.5" markerEnd="url(#overview-arrow)" />

    <rect className="box" x="200" y="14" width="140" height="44" rx="8" />
    <text className="title" x="270" y="30" textAnchor="middle" dominantBaseline="central">training process</text>
    <text className="sub" x="270" y="46" textAnchor="middle" dominantBaseline="central">runs natively</text>

    <rect className="box" x="200" y="72" width="140" height="44" rx="8" />
    <text className="title" x="270" y="88" textAnchor="middle" dominantBaseline="central">NVML thread</text>
    <text className="sub" x="270" y="104" textAnchor="middle" dominantBaseline="central">polls every 100ms</text>

    <line x1="340" y1="36" x2="392" y2="36" stroke="#B4B2A9" strokeWidth="0.5" markerEnd="url(#overview-arrow)" />

    <rect className="box" x="400" y="14" width="120" height="44" rx="8" />
    <text className="title" x="460" y="30" textAnchor="middle" dominantBaseline="central">stdout</text>
    <text className="sub" x="460" y="46" textAnchor="middle" dominantBaseline="central">direct to terminal</text>

    <line x1="340" y1="94" x2="392" y2="94" stroke="#B4B2A9" strokeWidth="0.5" markerEnd="url(#overview-arrow)" />

    <rect className="box" x="400" y="72" width="120" height="44" rx="8" />
    <text className="title" x="460" y="88" textAnchor="middle" dominantBaseline="central">power readings</text>
    <text className="sub" x="460" y="104" textAnchor="middle" dominantBaseline="central">(timestamp, watts)</text>

    <line x1="520" y1="58" x2="552" y2="58" stroke="#B4B2A9" strokeWidth="0.5" markerEnd="url(#overview-arrow)" />
    <line x1="460" y1="116" x2="460" y2="140" stroke="#B4B2A9" strokeWidth="0.5" />
    <line x1="460" y1="140" x2="560" y2="140" stroke="#B4B2A9" strokeWidth="0.5" />
    <line x1="560" y1="140" x2="560" y2="80" stroke="#B4B2A9" strokeWidth="0.5" />

    <rect className="accent" x="560" y="30" width="100" height="56" rx="8" />
    <text className="title" x="610" y="52" textAnchor="middle" dominantBaseline="central">summary</text>
    <text className="sub" x="610" y="70" textAnchor="middle" dominantBaseline="central">energy, power</text>

    <text className="sub" x="340" y="180" textAnchor="middle">Training and NVML polling run in parallel. Zero interaction. Zero overhead.</text>
  </svg>
</div>

<div className="sr-only">

How Matcha works: `matcha run your_command` spawns two parallel paths. The **training process** runs natively and writes **stdout** direct to the terminal. In parallel, an **NVML thread** polls GPU power every 100ms and records power readings as `(timestamp, watts)` tuples. When the training process exits, Matcha prints a **summary** line with total energy and average/peak power. Training and NVML polling run in parallel with zero interaction and zero overhead.

</div>

When the training process exits, Matcha computes total energy using trapezoidal integration of the power readings and prints a single summary line.

## What you need

- An NVIDIA GPU with drivers installed (`nvidia-smi` must work)
- Python 3.9+
- `pip` or `uv`

<ManualPageNav
  prev={{ href: '/docs', label: 'Home' }}
  next={{ href: '/docs/installation', label: 'Installation' }}
/>

</div>
