---
title: Multi-GPU
description: Measuring energy across multiple GPUs.
---

import { ManualPageNav } from '@/components/ManualPageNav'

<div className="home-flow">

# Multi-GPU

## Auto-detection

By default, Matcha detects all NVIDIA GPUs on the machine and sums power readings across all of them. No flags needed.

<div className="home-diagram">
  <svg width="100%" viewBox="0 0 680 260" xmlns="http://www.w3.org/2000/svg" aria-label="Multi-GPU Matcha commands">
    <style>
      {`
        text { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif; }
        .mono { font-size: 12px; font-weight: 400; fill: #2C2C2A; font-family: "SF Mono", "Fira Code", "Consolas", monospace; }
        .mono-dim { font-size: 12px; font-weight: 400; fill: #888780; font-family: "SF Mono", "Fira Code", "Consolas", monospace; }
        .mono-comment { font-size: 11px; font-weight: 400; fill: #B4B2A9; font-family: "SF Mono", "Fira Code", "Consolas", monospace; }
        .terminal { fill: #F8F7F4; stroke: #D3D1C7; stroke-width: 0.5; }
        .label { font-size: 11px; font-weight: 500; fill: #888780; letter-spacing: 0.5px; }
      `}
    </style>

    <text className="label" x="40" y="24">ALL GPUS (DEFAULT)</text>
    <rect className="terminal" x="40" y="34" width="600" height="56" rx="6" />
    <text className="mono-dim" x="56" y="54" dominantBaseline="central">$</text>
    <text className="mono" x="72" y="54" dominantBaseline="central">matcha run torchrun -​-standalone -​-nproc_per_node=8 train.py</text>
    <text className="mono-comment" x="56" y="74" dominantBaseline="central"># auto-detects all 8 GPUs, sums power across them</text>

    <text className="label" x="40" y="118">SPECIFIC GPUS</text>
    <rect className="terminal" x="40" y="128" width="600" height="56" rx="6" />
    <text className="mono-dim" x="56" y="148" dominantBaseline="central">$</text>
    <text className="mono" x="72" y="148" dominantBaseline="central">matcha run -​-gpus 0,1,2,3 torchrun -​-nproc_per_node=4 train.py</text>
    <text className="mono-comment" x="56" y="168" dominantBaseline="central"># only polls GPUs 0 through 3</text>

    <text className="label" x="40" y="212">OUTPUT (8xH100)</text>
    <rect className="terminal" x="40" y="222" width="600" height="30" rx="6" />
    <text className="mono" x="56" y="240" dominantBaseline="central">matcha_energy gpus:8x NVIDIA H100 80GB HBM3 total:2920000J (811Wh)</text>
  </svg>
</div>

## How it works

Every 100ms, Matcha reads `nvmlDeviceGetPowerUsage` for each monitored GPU and sums the values into a single power reading. Trapezoidal integration then computes total energy from these summed readings.

`avg_power` and `peak_power` in the output reflect the aggregate across all monitored GPUs. For example, 8 GPUs each drawing 500W would show `avg_power:4000W`.

<ManualPageNav
  prev={{ href: '/docs/per-step-energy', label: 'Per-Step Energy' }}
  next={{ href: '/docs/cli', label: 'CLI Reference' }}
/>

</div>
